function petShop(input){
    let dogFood = 2.5 * (input[0]);
    let catFood = 4 * (input[1]);
    let result = dogFood + catFood;
    console.log(result);
}

petShop(["5","4"]) // without this row